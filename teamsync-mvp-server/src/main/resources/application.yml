server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/teamsync_mvp?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 661213
    driver-class-name: com.mysql.cj.jdbc.Driver

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 100MB

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto

# AI API Configuration (DashScope/阿里云通义千问, OpenAI-compatible)
ai:
  api-key: ${AI_API_KEY:sk-ec7499e948e3422fbed46dbf8023658c}
  base-url: https://api.deepseek.com
  model: deepseek-v4-flash

# File upload path
upload:
  path: ${user.home}/teamsync-uploads

# Dify AI Agent 配置
dify:
  api-base-url: http://localhost/v1
  api-key: ${DIFY_API_KEY:app-YajsjGmOTpPFhOH8Xb41lTIx}

# AI Agent（Dify 等）调用后端 /api/ai/** 接口的专用密钥，不绑定用户会话、不过期
ai-integration:
  api-key: ${AI_INTEGRATION_API_KEY:teamsync-ai-key-2026}

# Token expiry in hours
token:
  expiry: 72

springdoc:
  swagger-ui:
    path: /swagger-ui.html
