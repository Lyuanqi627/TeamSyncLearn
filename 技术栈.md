这是一份基于前期讨论与优化，专为 **TeamSync Learn 一期（MVP 快速交付阶段）** 量身定制的最终版系统架构与技术设计文档。

本文档的核心思想是：**“架构做减法，业务做闭环”**。剥离重型中间件，聚焦“目标-日程-成果-AI评估”的核心链路，确保在极短时间内开发出可用的软件模型，同时在代码结构上为未来的企业级演进预留平滑升级的空间。

---

# TeamSync Learn 系统架构与技术设计文档（MVP 落地版）

**文档版本：** V1.0-MVP  
**适用阶段：** 一期 MVP（最小可行性产品）快速开发与验证  
**核心技术：** Vue3 + Vite + Spring Boot 3.2 + Spring AI + MySQL 8.0  

---

## 一、 架构设计理念与演进路线

### 1.1 核心设计原则
1. **MVP 优先，拒绝过度设计**：一期不引入 Redis、RabbitMQ、Elasticsearch、Milvus 等重型中间件，所有数据持久化与检索均基于 MySQL 8.0，降低部署与运维门槛。
2. **核心业务闭环**：集中资源打通“学习目标设定 $\rightarrow$ 日程规划 $\rightarrow$ 多模态成果上传 $\rightarrow$ AI 智能评估与总结”的核心链路。
3. **代码隔离，平滑演进**：虽然物理部署为单体，但逻辑上严格分层。未来升级 V2.0 时，只需替换基础设施实现（如将本地文件替换为 OSS，将 `@Async` 替换为 MQ），无需重构业务代码。

### 1.2 技术演进路线图
| 阶段 | 架构形态 | 核心组件 | 目标 |
| :--- | :--- | :--- | :--- |
| **V1.0 (当前 MVP)** | 轻量级单体 | MySQL + 本地磁盘 + Spring `@Async` + 内存分词 | **最快跑通核心 AI 业务闭环，验证产品价值** |
| **V2.0 (增长期)** | 增强型单体 | 引入 Redis (缓存/排行) + MinIO (对象存储) + RabbitMQ (异步削峰) | 支撑百人级团队，提升并发与响应速度 |
| **V3.0 (企业级)** | 微服务/云原生 | 引入 ES (全文检索/向量 RAG) + Milvus + K8s 部署 | 支撑海量数据、复杂 RAG 检索与高可用 |

---

## 二、 总体系统架构

### 2.1 轻量化系统架构图

```text
┌─────────────────────────────────────────────────────────────────┐
│                       前端表现层 (单应用多视图)                   │
│      Vue3 + Vite + Element Plus + ECharts + md-editor-v3        │
│   [成员视图：日程/成果/总结]       [管理视图：团队看板/词云分析]    │
└──────────────────────────────┬──────────────────────────────────┘
                               │ HTTP (Axios) + 简单 Token 拦截
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                    后端服务层 (Spring Boot 3.2 单体)              │
│  ┌────────────┐  ┌────────────┐  ┌─────────────┐  ┌─────────┐  │
│  │ 业务 API   │  │ AI 引擎    │  │ 异步线程池   │  │ 本地文件 │  │
│  │(日程/成果) │  │(Spring AI) │  │ (@Async)    │  │ 读写服务 │  │
│  └────────────┘  └────────────┘  └─────────────┘  └─────────┘  │
└──────────────────────────────┬──────────────────────────────────┘
                               │ JDBC
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                         数据存储层                                │
│                     MySQL 8.0 (唯一持久化存储)                    │
│         (用户、日程、成果、AI评估记录、AI总结、词云缓存)           │
└─────────────────────────────────────────────────────────────────┘
                               │ HTTP API
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                         AI 大模型层                               │
│               通义千问 / 智谱 GLM / OpenAI (按需切换)             │
└─────────────────────────────────────────────────────────────────┘
```

---

## 三、 前端技术设计

### 3.1 技术选型清单
| 分类 | 技术栈 | 版本 | 核心用途 |
| :--- | :--- | :--- | :--- |
| **核心框架** | Vue 3 + TypeScript | 3.4+ / 5.x | 采用 `<script setup>` 语法，保证类型安全。 |
| **构建工具** | Vite | 5.x | 极速冷启动与热更新。 |
| **UI 组件库** | Element Plus | 2.x | 提供成熟的表单、表格、弹窗等后台管理组件。 |
| **状态管理** | Pinia | 2.x | 管理全局用户状态、AI 加载状态。 |
| **路由** | Vue Router | 4.x | 通过路由元信息（`meta`）区分成员端与管理端权限。 |
| **可视化** | ECharts 5 + `echarts-wordcloud` | 5.x | 勤奋值折线图、目标达成度环形图、团队词云。 |
| **特色组件** | `md-editor-v3` / `vuedraggable` | 最新 | Markdown 成果编辑、日程看板拖拽流转。 |

### 3.2 工程目录结构 (扁平化单仓)
```text
teamsync-mvp-web/
├── src/
│   ├── api/           # 接口请求封装 (按业务模块划分)
│   ├── assets/        # 静态资源 (图标、默认头像)
│   ├── components/    # 通用组件 (MarkdownEditor, AiSummaryCard, WordCloud)
│   ├── composables/   # 组合式函数 (useAuth, useLoading)
│   ├── router/        # 路由配置 (包含简单的权限守卫)
│   ├── stores/        # Pinia 状态管理
│   ├── views/
│   │   ├── member/    # 成员端页面 (Dashboard, Schedule, Upload, Summary)
│   │   ├── admin/     # 管理端页面 (TeamBoard, WordCloud, MemberDetail, UserManage)
│   │   └── Login.vue  # 极简登录/注册页
│   ├── App.vue
│   └── main.ts
├── vite.config.ts
└── package.json
```

---

## 四、 后端技术设计

### 4.1 技术选型清单
| 分类 | 技术栈 | 版本 | 核心用途 |
| :--- | :--- | :--- | :--- |
| **核心框架** | Spring Boot | 3.2+ | 基于 JDK 17/21，提供自动配置与内嵌 Tomcat。 |
| **ORM 框架** | MyBatis-Plus | 3.5.x | 极简的单表 CRUD、分页插件、动态条件构造。 |
| **AI 框架** | Spring AI | 0.8+ | 统一大模型 API 调用、Prompt 模板管理、JSON 结构化输出映射。 |
| **数据库** | MySQL | 8.0 | 核心业务数据持久化。 |
| **异步处理** | Spring `@Async` | 内置 | 替代 MQ，利用内置线程池异步执行 AI 评估任务，防止 HTTP 超时。 |
| **工具库** | Hutool / MapStruct | 5.x / 1.5+ | 日期处理、文件操作、Entity 与 DTO 的快速转换。 |
| **接口文档** | Knife4j | 4.x | 自动生成 Swagger 接口文档，方便前后端联调。 |

### 4.2 工程目录结构 (单 Module 清晰分层)
```text
teamsync-mvp-server/
└── src/main/java/com/teamsync/
    ├── config/        # 配置类 (AsyncConfig, WebMvcConfig, SpringAiConfig)
    ├── controller/    # 接口层 (UserController, ScheduleController, AiController)
    ├── service/       # 业务逻辑层 (包含 AI Prompt 构建与调用逻辑)
    ├── mapper/        # 数据访问层 (MyBatis-Plus BaseMapper)
    ├── entity/        # 数据库实体类 (DO)
    ├── dto/           # 数据传输对象 (接收前端参数)
    ├── vo/            # 视图对象 (返回前端的数据结构)
    ├── common/        # 通用组件 (Result封装, 全局异常处理, UserContext, Roles)
    ├── security/      # Spring Security 认证与异常处理 (TokenAuthenticationFilter, 401/403处理器)
    └── TeamsyncMvpApplication.java
```

---

## 五、 核心功能实现方案

### 5.1 鉴权机制（Spring Security 6.x 三级权限）
采用 **Spring Security 6.x 无状态认证 + RBAC 三级角色**：
1. **会话**：登录成功生成 UUID Token，存入 Redis（key=token, value=userId, TTL=`token.expiry` 小时，默认 72）；裸 Token 放 `Authorization` 头（无 Bearer 前缀）。登出删除 Redis 键，服务端真正失效。
2. **认证**：`TokenAuthenticationFilter`（`OncePerRequestFilter`）读裸 Token → Redis 取 userId → **每请求查 DB 读 role**（角色变更即时生效）→ 构造 `Authentication` 写入 `SecurityContext`，并同步到 `UserContext`（ThreadLocal）。无效凭证不设认证，由 `RestAuthenticationEntryPoint`(401) / `RestAccessDeniedHandler`(403) 输出统一 JSON。
3. **AI 密钥旁路**：`/api/ai/**` 且 `Authorization == ai-integration.api-key` 合成 `ROLE_AI_BYPASS` 认证，**不填 UserContext**（`UserContext.getUserRole()==null` 即旁路，接口以显式 `userId` 参数定位用户）。
4. **授权**：URL 规则（`/api/admin/users/**`→SUPER_ADMIN；`/api/admin/**`→ADMIN/SUPER_ADMIN；`/api/**`→authenticated）+ `@EnableMethodSecurity` + `@PreAuthorize` 纵深防御。
5. **三角色**：`SUPER_ADMIN`（唯一最终管理员，可授予/收回他人 ADMIN、全量数据访问）、`ADMIN`（二级管理员，只读全量数据）、`MEMBER`（普通成员，仅访问本人数据）。业务写操作一律 owner-only。

### 5.2 AI 评估与总结引擎 (Spring AI 核心应用)
采用**异步线程池 + 结构化输出**，确保用户体验与数据准确性。

**核心代码逻辑：**
```java
@Service
public class AiEvaluationService {

    @Autowired private ChatClient chatClient;

    // 1. 异步执行，避免阻塞主线程导致 HTTP 超时
    @Async("aiTaskExecutor")
    public void evaluateAndSummarize(Long achievementId, String goal, String content) {
        
        // 2. 定义严格的 JSON 输出结构
        BeanOutputConverter<AiResultVO> converter = 
            new BeanOutputConverter<>(AiResultVO.class);

        // 3. 构建 Prompt
        String prompt = String.format("""
            你是一位严厉但鼓励人的学习导师。请评估以下学习成果。
            学习目标：{%s}
            成果内容：{%s}
            
            请进行评估并输出总结，严格按照以下 JSON 格式：
            %s
            """, goal, content, converter.getFormat());

        // 4. 调用大模型并自动反序列化为 Java 对象
        AiResultVO result = chatClient.prompt()
            .user(prompt)
            .call()
            .entity(AiResultVO.class);

        // 5. 持久化结果并更新日程状态
        saveAiResult(achievementId, result);
    }
}
```

### 5.3 词云分析 (内存计算替代 ES)
不引入 Elasticsearch，利用 MySQL 查询 + Java 内存分词实现：
1. 查询本周所有已完成日程的 `achievement` 文本内容。
2. 引入轻量级 Java 分词库（如 `Ansj` 或简单的正则/字典匹配）。
3. 在内存中过滤停用词（的、了、是、今天），使用 `Map<String, Integer>` 统计词频。
4. 取 Top 50 返回给前端 ECharts 渲染。

### 5.4 本地文件存储 (替代 MinIO/OSS)
1. **上传**：接收 `MultipartFile`，按 `yyyy/MM/dd/uuid.ext` 格式保存到服务器本地物理目录（如 `./uploads/`）。
2. **访问**：在 `WebMvcConfigurer` 中配置静态资源映射，使前端可通过 `http://domain/uploads/xxx.png` 直接访问图片。

---

## 六、 数据库设计 (MySQL 极简版)

仅保留 5 张核心业务表，去除一切冗余字段，确保快速建表与开发。

```sql
-- 1. 用户表 (极简)
CREATE TABLE sys_user (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) UNIQUE NOT NULL,
    role VARCHAR(20) DEFAULT 'MEMBER', -- MEMBER / ADMIN
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 2. 会话说明：不落库，存 Redis（key=token, value=userId, TTL 由 token.expiry 控制，默认 72h）

-- 3. 日程与目标表 (合并简化)
CREATE TABLE schedule (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    title VARCHAR(200) NOT NULL,
    goal_desc TEXT,          -- 关联的学习目标描述
    status TINYINT DEFAULT 0, -- 0未开始 1进行中 2已完成
    plan_date DATE NOT NULL
);

-- 4. 成果表
CREATE TABLE achievement (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    schedule_id BIGINT NOT NULL,
    content_type VARCHAR(20), -- TEXT / MARKDOWN / FILE
    content TEXT,             -- 文本或 Markdown 内容
    file_url VARCHAR(500),    -- 本地文件相对路径
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 5. AI 评估与总结表
CREATE TABLE ai_result (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    achievement_id BIGINT NOT NULL,
    user_id BIGINT NOT NULL,
    diligence_score INT,      -- 勤奋值/完成度评分 (0-100)
    ai_comment TEXT,          -- AI 点评与 Gap 分析
    ai_summary TEXT,          -- AI 核心知识点总结
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

---

## 七、 部署与运维 (一键容器化)

采用 Docker Compose 实现开发、测试与演示环境的一键启动，屏蔽环境差异。

```yaml
version: '3.8'
services:
  # 1. 数据库
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: teamsync_mvp
    ports: ["3306:3306"]
    volumes:
      - mysql_data:/var/lib/mysql
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql # 自动执行建表脚本

  # 2. 后端服务
  backend:
    build: ./teamsync-mvp-server
    ports: ["8080:8080"]
    depends_on: [mysql]
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/teamsync_mvp?useUnicode=true&characterEncoding=utf-8
      AI_API_KEY: ${AI_API_KEY} # 从宿主机环境变量读取大模型 Key，防止硬编码
    volumes:
      - ./uploads:/app/uploads  # 本地文件持久化映射

  # 3. 前端服务
  frontend:
    build: ./teamsync-mvp-web
    ports: ["80:80"]
    depends_on: [backend]

volumes:
  mysql_data:
```

---

## 八、 MVP 阶段开发排期建议 (敏捷冲刺)

假设投入 **1名全栈工程师**（或 1前端 + 1后端 并行），预计 **1周（5-7个工作日）** 完成 MVP 交付：

| 阶段 | 核心任务 | 预计耗时 |
| :--- | :--- | :--- |
| **Day 1** | 数据库建表、Spring Boot 骨架搭建、MyBatis-Plus 配置、极简登录拦截器实现。 | 1 天 |
| **Day 2** | 日程 CRUD 接口、成果上传接口（含本地文件存储）、前端日程看板与上传页面联调。 | 1.5 天 |
| **Day 3** | 接入 Spring AI，调试“评估与总结”Prompt，实现 `@Async` 异步调用与 JSON 结构化解析。 | 1.5 天 |
| **Day 4** | 前端 AI 结果展示（总结卡片）、管理员数据大盘（MySQL 聚合查询）、词云内存统计与渲染。 | 1.5 天 |
| **Day 5** | 全局 UI 走查、Bug 修复、Docker Compose 打包部署、内部演示验收。 | 1 天 |

---
**文档结语：**  
本设计文档通过“做减法”确保了 TeamSync Learn 能够以极低的成本快速面世并验证 AI 赋能学习的业务逻辑。当产品获得初步验证并积累一定用户量后，可严格按照“1.2 技术演进路线图”平滑引入 Redis、MQ 和向量数据库，向企业级架构无缝过渡。


加入推送测试